※注意※ zip圧縮時は、単一フォルダでなく、ルートフォルダ上の全ファイルを指定して圧縮すること

例：win-x64フォルダにpublishファイルがあるとき
win-x64フォルダを圧縮するのではなく、win-x64フォルダ上の全ファイルを指定して圧縮する。

展開したとき、出力先フォルダに"win-x64"フォルダが出来るのではなく、出力先フォルダに全publishファイルが配置されるようにするため。
